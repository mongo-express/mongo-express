---
declared-services:
  db-mongo:
    label: mongodb
    plan: 100
applications:
  memory: 128M
  name: mongo-express
  host: mongo-express
  services:
  - db-mongo
  env:
    NODE_ENV: cloud-foundry
